spring {
    jpa {
        hibernate {
            show_sql = true
            ddl_auto = 'none'
        }
    }
    datasource {
        url = 'jdbc:h2:~/workspace/community_board/db/db;MODE=MySQL'
        username = 'test'
        password = 'test'
    }
}
server {
    port = '8080'
}
logging {
    level {
        root = 'DEBUG'
    }
}