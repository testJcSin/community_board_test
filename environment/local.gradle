spring {
    jpa {
        hibernate {
            show_sql = true
            ddl_auto = 'update '
        }
    }
    datasource {
        driverClassName = 'org.h2.Driver'
        url = 'jdbc:h2:mem:AZ;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=TRUE;MODE=MySQL'
        username = 'sa'
        password = ''

    }
}
server {
    port = '8080'
}
logging {
    level {
        root = 'DEBUG'
    }
}